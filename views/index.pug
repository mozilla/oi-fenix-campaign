extends layout

block content
  .container
    h1 Some form...

    form(action="/create", method="POST", enctype="multipart/form-data")
      label(for="test") Test
      input(id="test", type="text", name="test_field")
      input(type="file", name="screenshot")
      button(type="submit") Create Issue
